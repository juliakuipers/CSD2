/*
enum stores a number in a name so:
  enum WaveformType {
  SINE =0,
  SAW = 1,
  SQUARE = 2
  SIZE = 3
}

Static void:
  executes the function it holds everytime its called

i will need a buffer at first to fill with the sigmoid 
*/
